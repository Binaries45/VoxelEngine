//! the renderer resource; handles all rendering

const Renderer = @This();

// TODO : store all unique meshes for rendering
// Meshes: MeshStorage,

// TODO : this will hold on to all meshes, handle querying all entities with a mesh and transform, applying projections and uploading data to the gpu
