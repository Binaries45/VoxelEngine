// pub const Entities= @import("Entities.zig");
// pub const Component = @import("component.zig");
// pub const Resource = @import("Resource.zig");

test "ecs" {
    // const std = @import("std");
    // var gpa: std.heap.DebugAllocator(.{}) = .init;
    // defer _ = gpa.deinit();
    // const alloc = gpa.allocator();
    //
    // var world = try Entities.init(alloc);
    // defer world.deinit();
    //
    // const player = try world.new();
    //
    // // Define component types, any Zig type will do!
    // // A location component.
    // const Location = struct {
    //     x: f32 = 0,
    //     y: f32 = 0,
    //     z: f32 = 0,
    // };
    //
    // const Name = []const u8;
    //
    // try world.setComponent(player, "Name", @as(Name, "jane")); // add Name component
    // try world.setComponent(player, "Location", Location{}); // add Location component
    // try world.setComponent(player, "Name", @as(Name, "joe")); // update Name component
    //
    // try std.testing.expectEqual(Location{}, world.getComponent(player, "Location", Location).?);
    // try std.testing.expectEqual(@as(Name, "joe"), world.getComponent(player, "Name", Name).?);
    //
    // try world.removeComponent(player, "Location");
    // try std.testing.expect(world.getComponent(player, "Location", Location) == null);
    //
    // try world.remove(player);

    // run other tests
    // _ = Entities;
    // _ = Component;
    // _ = Resource;
}